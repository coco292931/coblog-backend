# 更新日志

本文档记录 `coblog-backend` 的主要变更。

---

## 2026-06-30

### 新增

- 新增邮箱验证码体系，支持注册、邮箱登录、找回密码等多种用途区分。
- 新增账户激活流程：注册生成激活 token，未激活用户登录时可自动重发激活邮件。
- 新增文章完整编辑链路：创建、修改、删除、获取编辑回填内容。
- 新增 Markdown 渲染预览接口 `POST /api/markdown/render`。
- 新增 RSS 自动生成能力，支持基于 `token` 控制深度内容可见范围，并支持 `category`、`tag` 筛选。
- 新增图片上传压缩能力：图片类型白名单、压缩阈值、最大宽度、压缩质量配置。

### 调整

- 文章保存策略调整为 **Markdown 优先**：存在 `md_content` 时由后端统一渲染 HTML。
- 扩展配置项，新增/完善 `smtp`、`site`、`fileobject.compress_*` 等配置。
- 调整 CORS 策略，增强本地、局域网和正式域名访问兼容性。
- 补充并重写 `README.md`，同步当前后端能力、配置方式与部署说明。

### 修复与优化

- 优化认证和用户相关服务逻辑，包括密码重置、RSS Token 重置等能力衔接。
- 优化文件上传返回结果，支持优先返回压缩图并保留原图地址。
- 优化文章详情/编辑读取逻辑，公开接口不再返回 Markdown 原文，编辑接口单独提供完整内容。
- 对存量账户执行激活状态回填，降低升级后旧账户不可登录的风险。

### 升级提示

- 需要同步更新 `configs/configs/appConfigs.yaml`，补齐新增配置项。
- 若启用邮件相关功能，必须正确配置 SMTP 服务。
- 若启用图片压缩，需确保上传目录可写，且配置中的 `fileobject.dir` 指向实际目录。

---

## 历史更新汇总

- 完成基于 Go、Gin、GORM 的基础分层后端架构。
- 实现自定义 Token 认证与权限位图校验机制。
- 完成基础登录、用户资料、权限控制与站点信息接口。
- 逐步完善文章列表读取、深度内容区分、搜索范围与查询条件处理。
- 对数据库连接、本地开发配置、Token 有效期与 CORS 问题做过多轮修正和兼容处理。
- 补充测试、文档和若干工程性整理工作。
